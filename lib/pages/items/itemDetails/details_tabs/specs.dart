import 'package:flutter/material.dart';

class Specs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Padding(
        padding: const EdgeInsets.all(14.0),
        child: new Center(
          child: new Text("Specs"),
        ),
      ),
    );
  }
}
